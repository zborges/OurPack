@items.each do |item|
  {
    id: item.id,
    name: item.name,
    description: item.description,
    weight: item.weight,
    quantity: item.quantity,
    url: item.url,
    category: item.category
  }
end